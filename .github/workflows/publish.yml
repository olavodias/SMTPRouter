name: Build, Test, and Publish

on:
  workflow_dispatch: # Allow running the workflow manually from the GitHub UI
  push:
    tags:
    - 'v*.*.*'

jobs:
  build:

    runs-on: windows-latest

    permissions:
      contents: read
      #packages: write

    steps:
    - uses: actions/checkout@v5
    - name: Setup .NET 8.0
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 8.0.x
    - name: Extract Version from Tag
      id: tag
      uses: actions/github-script@v7
      with:
        script: |
          const version = context.ref.replace('refs/tags/', '');
          return version.slice(1);
    - name: Print Results
      run: echo ${{ steps.tag.outputs.result }}

#    - name: Change Package Version
#      working-directory: ./src
#      run: |
#        sed -i -e "s/<Version>[a-zA-Z0-9.-]*<\/Version>/<Version>${{ steps.tag.outputs.result }}<\/Version>/g" SMTPRouter.Abstractions/SMTPRouter.Abstractions.csproj
#        sed -i -e "s/<Version>[a-zA-Z0-9.-]*<\/Version>/<Version>${{ steps.tag.outputs.result }}<\/Version>/g" SMTPRouter.Core/SMTPRouter.Core.csproj
#        sed -i -e "s/<Version>[a-zA-Z0-9.-]*<\/Version>/<Version>${{ steps.tag.outputs.result }}<\/Version>/g" SMTPRouter.Listener/SMTPRouter.Listener.csproj
#        sed -i -e "s/<Version>[a-zA-Z0-9.-]*<\/Version>/<Version>${{ steps.tag.outputs.result }}<\/Version>/g" SMTPRouter.Router/SMTPRouter.Router.csproj

#    - name: Restore Dependencies
#      working-directory: ./src
#      run: dotnet restore
#    - name: Build
#      working-directory: ./src
#      run: dotnet build --configuration Release --no-restore
#    - name: Test
#      working-directory: ./src
#      run: |
#        dotnet test --configuration Release --no-restore --no-build --verbosity normal
#    - name: Build Project and Generate NuGet package
#      run: |
#        dotnet pack --configuration Release --output $GITHUB_WORKSPACE/out src/SmtpServer/SmtpServer.csproj
#    - name: Push NuGet Package
#      run: |
#        cd $GITHUB_WORKSPACE/out
#        dotnet nuget push *.nupkg --source https://api.nuget.org/v3/index.json --skip-duplicate --no-symbols
